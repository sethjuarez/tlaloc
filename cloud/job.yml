$schema: https://azuremlsdk2.blob.core.windows.net/latest/commandJob.schema.json
experiment_name: my_experiment # default to top level folder dir?
code: 
  local_path: ./src # can the dropdown have the folders available?
command: >-
  python train.py 
  --data {inputs.training_data}
# would it be usfule to look in
# the definitions in the current
# project to determine the right
# env, target, and dataset?
environment: azureml:xgboost-environment:1 # have an item in the dropbox to create new inline?
compute:
  target: azureml:goazurego
inputs:
  training_data:
    data: azureml:irisdata:1
    mode: mount