$schema: https://azuremlsdk2.blob.core.windows.net/latest/managedOnlineEndpoint.schema.json
name: my-endpoint
type: online
auth_mode: key
traffic:
  red: 80
  blue: 20
deployments:
  # auto completions that point to deployment files?
  # (I also think this doesn't work)
  red: deployment.yml
  blue: deployment2.yml