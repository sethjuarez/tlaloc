$schema: https://azuremlsdk2.blob.core.windows.net/latest/model.schema.json
name: my-model # default to workspace folder?
version: 1
description: this is a sample model
tags:
  foo: bar
local_path: ./mnist.pt # find first file with .pt, .pth, .onnx, .pb (etc.)
# is this path a remote path of some sort?
path: ./